# QuickBox（快取柜）需求文档 v2.0

## 1. 项目概述

### 1.1 项目背景
QuickBox（快取柜）是一个临时文件共享服务的后端系统，旨在为用户提供安全、高效、临时的文件上传和下载服务。文件在7天后自动删除，或在下载后立即删除，适用于临时文件共享场景。

### 1.2 项目目标
- 提供简单易用的文件上传下载API接口
- 支持大文件分片上传，提高上传稳定性和效率
- 通过取件码机制保证文件访问的安全性
- 自动清理过期文件，避免存储空间浪费
- 提供高可用、可扩展的后端服务
- **二期新增：支持文件夹批量上传**
- **二期新增：支持多种存储后端（本地、对象存储、WebDAV、NAS、GPFS等）**
- **二期新增：完善的Docker和Docker Compose部署方案**

### 1.3 项目范围
- 文件上传服务（支持分片上传）
- **文件夹上传服务（支持文件夹结构保持）**
- 文件下载服务（通过取件码访问）
- 取件码生成和管理
- 文件元数据管理
- 会话和过期管理
- 系统监控和日志记录
- **存储后端抽象层，支持多种存储系统**
- **容器化部署支持**

## 2. 功能性需求

### 2.1 文件上传功能
#### 2.1.1 分片上传
- 支持大文件分片上传，适配vue-simple-uploader前端组件
- 每分片大小可配置，默认根据前端组件要求
- 支持上传进度实时查询
- 支持断点续传（通过已上传分片列表）

#### 2.1.2 **文件夹上传**
- **支持文件夹结构上传，保持原始目录层级**
- **支持文件夹压缩包（ZIP）上传并自动解压**
- **支持大文件夹分片上传**
- **文件夹上传进度跟踪**
- **文件夹内文件数量限制配置**
- **文件夹总大小限制配置**

#### 2.1.3 上传会话管理
- 每个上传会话具有唯一ID
- 会话有效期24小时，过期自动清理
- 会话状态实时更新（已上传分片数、进度等）
- **支持文件夹上传会话管理**

#### 2.1.4 文件合并
- 所有分片上传完成后自动合并为完整文件
- 合并过程使用流式操作，减少内存占用
- 合并后自动清理临时分片文件
- **支持文件夹分片合并和目录结构重建**

### 2.2 文件下载功能
#### 2.2.1 取件码机制
- 生成6位大写字母随机取件码（约3亿种组合）
- 取件码在Redis中存储，7天自动过期
- 取件码单次使用，下载后立即失效
- 支持取件码状态查询
- **支持文件夹下载取件码，可下载整个文件夹（ZIP打包）**

#### 2.2.2 文件下载
- 通过取件码下载文件
- 支持文件流下载，自动设置Content-Disposition头
- 下载后文件自动删除（可选配置）
- 支持文件名URL编码，防止特殊字符问题
- **支持文件夹下载，自动打包为ZIP格式**
- **支持文件夹内部分文件下载**

#### 2.2.3 文件信息查询
- 通过取件码查询文件元数据
- 返回文件名、大小、修改时间等信息
- 返回下载URL
- **支持文件夹信息查询，返回文件树结构**

### 2.3 **存储系统功能**
#### 2.3.1 **存储抽象层**
- **统一存储接口设计，支持多种存储后端**
- **存储策略模式实现，运行时动态切换**
- **存储后端自动发现和加载**
- **存储健康状态检查**

#### 2.3.2 **支持的存储后端**
- **本地文件系统（增强版）**
    - Windows和Linux路径支持
    - 符号链接处理
    - 权限管理
- **对象存储**
    - AWS S3兼容（MinIO, Ceph RGW等）
    - 分片上传支持
    - 存储桶策略管理
- **WebDAV**
    - 标准WebDAV协议支持
    - 认证机制（Basic, Digest）
    - 目录列表支持
- **NAS（网络附加存储）**
    - NFS协议支持
    - SMB/CIFS协议支持
    - 挂载点管理
- **GPFS（IBM Spectrum Scale）**
    - 并行文件系统支持
    - 高性能IO操作
    - 集群存储支持
- **其他存储系统**
    - FTP/SFTP
    - 阿里云OSS
    - 腾讯云COS
    - 华为云OBS

#### 2.3.3 **存储策略**
- **主备存储策略**
- **分级存储策略**
- **存储后端负载均衡**
- **存储空间自动扩展**
- **存储迁移工具**

#### 2.3.4 **存储监控**
- **存储空间使用情况监控**
- **存储性能指标收集**
- **存储后端健康状态告警**
- **存储操作审计日志**

### 2.4 系统管理功能
#### 2.4.1 存储管理
- 支持本地文件系统存储
- 支持Windows和Linux路径配置
- 分片文件和最终文件分离存储
- 存储目录结构化管理
- **支持多存储后端统一管理**
- **存储策略配置管理**

#### 2.4.2 清理机制
- 定时清理过期上传会话（24小时未完成）
- 定时清理过期文件（7天未下载）
- 下载后立即清理文件（可选）
- Redis自动过期清理
- **跨存储后端统一清理机制**
- **存储空间回收优化**

#### 2.4.3 监控和日志
- 上传下载操作日志记录
- 系统异常日志记录
- 存储空间监控
- 性能指标监控
- **存储后端监控指标**
- **容器化环境监控**

## 3. 非功能性需求

### 3.1 性能需求
- 支持并发文件上传下载
- 大文件上传内存占用优化
- Redis缓存响应时间<10ms
- 文件合并过程不阻塞其他操作
- **多存储后端并发性能优化**
- **文件夹上传下载性能优化**

### 3.2 可靠性需求
- 系统可用性≥99.9%
- 数据一致性保证
- 异常情况下的数据完整性
- 断电等异常情况的恢复能力
- **多存储后端故障切换**
- **数据冗余和备份机制**

### 3.3 安全性需求
- 取件码单次使用，防止重复下载
- 文件路径不暴露敏感信息
- 防止目录遍历攻击
- 支持文件名安全验证
- **跨存储后端的安全认证**
- **数据传输加密支持**

### 3.4 可维护性需求
- 模块化设计，便于功能扩展
- 清晰的代码结构和注释
- 详细的日志记录
- 配置外部化，便于部署调整
- **存储后端插件化架构**
- **容器化部署标准化**

### 3.5 兼容性需求
- 支持Spring Boot 3.x
- 支持Java 17+
- 支持Redis 6.0+
- 适配主流前端上传组件
- **兼容多种存储协议**
- **跨平台容器化支持**

## 4. 系统架构

### 4.1 技术架构
#### 4.1.1 后端框架
- Spring Boot 3.5.4
- Spring Web MVC
- Spring Data Redis
- Lombok
- **Spring Boot Actuator（监控）**
- **Docker Java Client API**

#### 4.1.2 数据存储
- Redis 6.0+：存储取件码状态和会话信息
- **存储抽象层：支持多种存储后端**
    - 本地文件系统
    - AWS S3 SDK / MinIO Client
    - Apache HttpClient（WebDAV）
    - JCIFS（SMB/CIFS）
    - NFS Client
    - 各云存储SDK

#### 4.1.3 构建和部署
- Maven 3.6+
- JDK 17+
- **支持Docker容器化部署**
- **支持Docker Compose多服务编排**
- **支持Kubernetes部署（未来扩展）**

### 4.2 系统架构图
```
客户端 → Spring Boot应用 → Redis缓存 → 存储抽象层
                                          ↓
                        ┌─────────────────────────────────┐
                        │       存储后端适配器            │
                        ├─────────────────────────────────┤
                        │ 本地  │ 对象存储 │ WebDAV │ NAS │
                        └─────────────────────────────────┘
```

### 4.3 组件架构
```
┌─────────────────────────────────────────────┐
│               控制器层 (Controller)          │
│  • FileUploadController                     │
│  • FolderUploadController（新增）           │
│  • StorageAdminController（新增）           │
├─────────────────────────────────────────────┤
│               服务层 (Service)               │
│  • FileUploadService (接口)                 │
│  • FileUploadServiceImpl (实现)             │
│  • FolderUploadService（新增）              │
│  • StorageService（新增接口）               │
│  • StorageStrategy（新增策略接口）          │
├─────────────────────────────────────────────┤
│               存储适配层（新增）             │
│  • LocalStorageAdapter（本地存储）          │
│  • S3StorageAdapter（对象存储）             │
│  • WebDAVStorageAdapter（WebDAV）          │
│  • NASStorageAdapter（NAS存储）             │
│  • GPFSStorageAdapter（GPFS）               │
├─────────────────────────────────────────────┤
│               数据层 (Model)                 │
│  • DTO: FileInfo, FolderInfo（新增）        │
│  • Request: ChunkUploadRequest, FileCheck   │
│  • Response: UploadSession, UploadProgress  │
├─────────────────────────────────────────────┤
│               配置层 (Config)                │
│  • FileStorageConfig                        │
│  • RedisConfig                              │
│  • CorsConfig                               │
│  • StorageAdapterConfig（新增）             │
│  • DockerConfig（新增）                     │
├─────────────────────────────────────────────┤
│               异常处理 (Exception)           │
│  • BusinessException                        │
│  • StorageException（新增）                 │
│  • GlobalExceptionHandler                   │
└─────────────────────────────────────────────┘
```

## 5. 接口规范

### 5.1 RESTful API设计
#### 5.1.1 接口约定
- 所有API以`/api`开头
- 使用JSON作为数据交换格式
- HTTP状态码标准化
- 统一响应格式

#### 5.1.2 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 5.2 核心接口列表
#### 5.2.1 上传相关接口
1. **初始化上传会话**
    - 方法：POST `/api/upload/init`
    - 功能：生成取件码，创建上传会话

2. **上传分片**
    - 方法：POST `/api/upload/upload`
    - 功能：上传单个分片文件

3. **查询上传进度**
    - 方法：GET `/api/upload/progress/{uploadId}`
    - 功能：查询上传进度和已上传分片

4. **合并分片**
    - 方法：POST `/api/upload/merge`
    - 功能：合并所有分片为完整文件

5. **初始化文件夹上传（新增）**
    - 方法：POST `/api/upload/folder/init`
    - 功能：创建文件夹上传会话
    - 请求体：
      ```json
      {
        "folderName": "project_docs",
        "totalFiles": 10,
        "totalSize": 104857600,
        "structure": "base64编码的目录结构"
      }
      ```

6. **上传文件夹分片（新增）**
    - 方法：POST `/api/upload/folder/chunk`
    - 功能：上传文件夹的分片数据
    - 支持包含目录结构的压缩包

7. **合并文件夹分片（新增）**
    - 方法：POST `/api/upload/folder/merge`
    - 功能：合并文件夹分片，重建目录结构

#### 5.2.2 下载相关接口
1. **获取文件信息**
    - 方法：GET `/api/upload/info/{accessCode}`
    - 功能：查询文件元数据

2. **下载文件**
    - 方法：GET `/api/upload/download/{accessCode}`
    - 功能：下载文件流

3. **获取文件夹信息（新增）**
    - 方法：GET `/api/upload/folder/info/{accessCode}`
    - 功能：查询文件夹结构和文件列表
    - 响应：
      ```json
      {
        "folderName": "project_docs",
        "totalFiles": 10,
        "totalSize": 104857600,
        "fileTree": [
          {
            "name": "docs",
            "type": "directory",
            "children": [...]
          }
        ]
      }
      ```

4. **下载文件夹（新增）**
    - 方法：GET `/api/upload/folder/download/{accessCode}`
    - 功能：下载整个文件夹（ZIP打包）
    - 可选参数：`?file=path/to/file` 下载单个文件

#### 5.2.3 存储管理接口（新增）
1. **存储后端列表**
    - 方法：GET `/api/storage/backends`
    - 功能：获取已配置的存储后端

2. **存储后端状态**
    - 方法：GET `/api/storage/status/{backendId}`
    - 功能：查询存储后端健康状态

3. **切换存储策略**
    - 方法：POST `/api/storage/strategy`
    - 功能：动态切换存储策略

4. **存储统计信息**
    - 方法：GET `/api/storage/stats`
    - 功能：获取各存储后端使用统计

### 5.3 接口详细说明
（详细接口参数和响应格式见项目README.md或API文档）

## 6. 数据模型

### 6.1 取件码数据
```java
{
  "accessCode": "ABCDEF",      // 6位取件码
  "filename": "example.zip",   // 文件名
  "folderName": "project",     // 文件夹名（新增）
  "isFolder": false,           // 是否为文件夹（新增）
  "filePath": "/files/ABCDEF", // 文件存储路径
  "folderPath": "/folders/ABCDEF", // 文件夹路径（新增）
  "fileSize": 10485760,        // 文件大小
  "totalFiles": 1,             // 文件数量（新增）
  "structure": "base64结构",    // 文件夹结构（新增）
  "createdAt": "2024-01-01T10:00:00Z", // 创建时间
  "expiresAt": "2024-01-08T10:00:00Z", // 过期时间
  "downloaded": false,         // 是否已下载
  "storageBackend": "local",   // 存储后端（新增）
  "storageLocation": "..."     // 存储位置（新增）
}
```

### 6.2 上传会话数据
```java
{
  "uploadId": "UUID",          // 上传会话ID
  "accessCode": "ABCDEF",      // 关联的取件码
  "filename": "example.zip",   // 文件名
  "folderName": "project",     // 文件夹名（新增）
  "isFolder": false,           // 是否为文件夹上传（新增）
  "totalSize": 10485760,       // 文件总大小
  "totalChunks": 5,            // 总分片数
  "uploadedChunks": [1, 2, 3], // 已上传分片列表
  "chunkPath": "/chunks/UUID", // 分片存储路径
  "createdAt": "2024-01-01T10:00:00Z", // 创建时间
  "expiresAt": "2024-01-02T10:00:00Z",  // 会话过期时间
  "storageBackend": "s3",      // 使用的存储后端（新增）
  "folderStructure": {...}     // 文件夹结构（新增）
}
```

### 6.3 存储后端配置（新增）
```java
{
  "backendId": "s3-primary",
  "type": "S3",
  "name": "主要对象存储",
  "enabled": true,
  "priority": 1,
  "config": {
    "endpoint": "https://s3.amazonaws.com",
    "accessKey": "AKIA...",
    "secretKey": "...",
    "bucket": "quickbox",
    "region": "us-east-1"
  },
  "healthStatus": "HEALTHY",
  "usedSpace": 1073741824,
  "totalSpace": 1099511627776,
  "lastCheckTime": "2024-01-01T10:00:00Z"
}
```

## 7. 配置管理

### 7.1 应用配置 (application.properties)
```properties
# 服务器配置
server.port=8089
server.servlet.context-path=/

# Redis配置
redis.host=localhost
redis.port=16379
redis.password=
redis.database=0

# 文件上传配置
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# 文件存储配置
file.storage.base-path=/home/ubuntu/quick_box/box_data
file.storage.chunk-path=chunks
file.storage.file-path=files

# 文件夹上传配置（新增）
folder.upload.max-files=1000
folder.upload.max-total-size=10GB
folder.upload.keep-structure=true
folder.upload.auto-zip=true

# 过期时间配置（单位：秒）
upload.session.expire-time=86400    # 24小时
access.code.expire-time=604800      # 7天

# 存储策略配置（新增）
storage.strategy.type=PRIMARY_BACKUP  # PRIMARY_BACKUP, LOAD_BALANCE, TIERED
storage.primary.backend=local
storage.backup.backends=s3,webdav
storage.health-check.interval=30000   # 30秒
storage.retry.max-attempts=3
storage.retry.delay=1000

# 本地存储配置（新增）
storage.local.base-path=${file.storage.base-path}
storage.local.chunk-path=${file.storage.chunk-path}
storage.local.file-path=${file.storage.file-path}

# S3存储配置（新增）
storage.s3.enabled=false
storage.s3.endpoint=https://play.min.io
storage.s3.access-key=minioadmin
storage.s3.secret-key=minioadmin
storage.s3.bucket=quickbox
storage.s3.region=us-east-1

# WebDAV存储配置（新增）
storage.webdav.enabled=false
storage.webdav.url=http://localhost:8080/webdav
storage.webdav.username=admin
storage.webdav.password=admin
storage.webdav.base-path=/quickbox

# NAS存储配置（新增）
storage.nas.enabled=false
storage.nas.type=NFS  # NFS or SMB
storage.nas.server=nas.example.com
storage.nas.share=/data/quickbox
storage.nas.mount-point=/mnt/quickbox

# 监控配置（新增）
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.metrics.export.prometheus.enabled=true
management.endpoint.health.show-details=always
```

### 7.2 环境配置
- **开发环境**：本地运行，使用本地Redis
- **测试环境**：模拟生产环境配置
- **生产环境**：独立服务器，高可用配置
- **Docker环境**：容器化部署配置

### 7.3 Docker配置（新增）
#### docker-compose.yml
```yaml
version: '3.8'

services:
  quickbox:
    image: quickbox:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quickbox
    restart: unless-stopped
    ports:
      - "8089:8089"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - STORAGE_STRATEGY=local
    volumes:
      - ./data/box_data:/app/box_data
      - ./logs:/app/logs
      - ./config:/app/config
    depends_on:
      - redis
    networks:
      - quickbox-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8089/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: quickbox-redis
    restart: unless-stopped
    ports:
      - "16379:6379"
    volumes:
      - ./data/redis:/data
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf
    command: redis-server /usr/local/etc/redis/redis.conf
    networks:
      - quickbox-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  minio:
    image: minio/minio:latest
    container_name: quickbox-minio
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    volumes:
      - ./data/minio:/data
    command: server /data --console-address ":9001"
    networks:
      - quickbox-network

  nginx:
    image: nginx:alpine
    container_name: quickbox-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - quickbox
    networks:
      - quickbox-network

networks:
  quickbox-network:
    driver: bridge

volumes:
  box_data:
  redis_data:
  minio_data:
```

#### Dockerfile
```dockerfile
# 构建阶段
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline
COPY src ./src
RUN mvn clean package -DskipTests

# 运行阶段
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app

# 安装必要的工具
RUN apk add --no-cache curl bash

# 创建非root用户
RUN addgroup -S quickbox && adduser -S quickbox -G quickbox
USER quickbox

# 复制构建产物
COPY --from=build /app/target/*.jar app.jar
COPY --chown=quickbox:quickbox docker-entrypoint.sh .
COPY --chown=quickbox:quickbox config/ ./config/

# 创建数据目录
RUN mkdir -p /app/data /app/logs && \
    chown -R quickbox:quickbox /app/data /app/logs

# 暴露端口
EXPOSE 8089

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8089/actuator/health || exit 1

# 启动脚本
ENTRYPOINT ["./docker-entrypoint.sh"]
```

## 8. 部署方案

### 8.1 部署架构 v2.0
```
quickbox-deploy/
├── docker/                    # Docker相关文件
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── docker-compose.prod.yml
│   ├── docker-entrypoint.sh
│   └── nginx/
│       └── nginx.conf
├── config/                    # 配置文件
│   ├── application-docker.properties
│   ├── application-prod.properties
│   ├── redis.conf
│   └── storage/
│       ├── local.properties
│       ├── s3.properties
│       └── webdav.properties
├── scripts/                   # 部署脚本
│   ├── deploy.sh
│   ├── backup.sh
│   ├── migrate-storage.sh
│   └── health-check.sh
├── data/                      # 数据目录（挂载卷）
│   ├── box_data/             # 本地存储数据
│   │   ├── chunks/
│   │   ├── files/
│   │   └── folders/
│   ├── redis/                # Redis数据
│   ├── minio/                # MinIO数据
│   └── logs/                 # 应用日志
├── ssl/                       # SSL证书
└── backup/                    # 备份文件
```

### 8.2 部署步骤 v2.0
#### 8.2.1 传统部署
1. **环境准备**
    - 安装JDK 17+
    - 安装Redis 6.0+
    - 配置系统环境变量
    - 安装存储后端客户端（可选）

2. **应用部署**
    - 编译打包：`mvn clean package`
    - 复制部署文件到目标目录
    - 修改配置文件
    - 启动应用

3. **服务管理**
    - 启动：`quick-box start`
    - 停止：`quick-box stop`
    - 重启：`quick-box restart`
    - 状态查看：`quick-box status`
    - 日志查看：`quick-box logs`

#### 8.2.2 Docker部署（新增）
1. **环境准备**
   ```bash
   # 安装Docker和Docker Compose
   sudo apt-get update
   sudo apt-get install docker.io docker-compose
   
   # 创建部署目录
   mkdir -p /opt/quickbox
   cd /opt/quickbox
   ```

2. **配置文件准备**
   ```bash
   # 复制配置文件
   cp -r config/ /opt/quickbox/
   cp docker-compose.yml /opt/quickbox/
   
   # 修改配置
   vim config/application-docker.properties
   ```

3. **启动服务**
   ```bash
   # 开发环境
   docker-compose up -d
   
   # 生产环境
   docker-compose -f docker-compose.prod.yml up -d
   ```

4. **服务管理**
   ```bash
   # 查看服务状态
   docker-compose ps
   
   # 查看日志
   docker-compose logs -f quickbox
   
   # 重启服务
   docker-compose restart quickbox
   
   # 停止服务
   docker-compose down
   ```

#### 8.2.3 存储后端配置（新增）
1. **配置本地存储**
   ```bash
   # 确保数据目录存在并具有正确权限
   mkdir -p /opt/quickbox/data/box_data
   chown -R 1000:1000 /opt/quickbox/data
   ```

2. **配置S3/MinIO存储**
   ```bash
   # 启动MinIO（如果使用本地MinIO）
   docker-compose up -d minio
   
   # 访问MinIO控制台创建bucket
   # http://localhost:9001
   
   # 配置S3连接信息
   vim config/storage/s3.properties
   ```

3. **配置WebDAV存储**
   ```bash
   # 安装WebDAV服务器（可选）
   # 配置WebDAV连接信息
   vim config/storage/webdav.properties
   ```

### 8.3 监控和维护 v2.0
- 日志文件监控（容器日志和文件日志）
- 存储空间监控（各存储后端）
- 性能指标监控（Prometheus集成）
- 容器健康状态监控
- 定期备份和清理
- 存储迁移和扩容

## 9. 安全考虑

### 9.1 访问安全
- 取件码单次使用机制
- 文件下载后自动删除
- 防止暴力破解取件码
- 文件路径安全验证
- **容器网络隔离**
- **存储后端认证加固**

### 9.2 数据安全
- 文件存储隔离
- 敏感信息不记录
- 异常情况数据清理
- 防止数据泄露
- **存储后端数据传输加密**
- **容器数据卷加密（可选）**

### 9.3 系统安全
- 输入参数验证
- 文件类型限制
- 路径遍历防护
- 异常信息处理
- **容器安全扫描**
- **镜像签名验证**

### 9.4 网络安全（新增）
- **容器间网络隔离**
- **API访问限流**
- **SSL/TLS加密传输**
- **防火墙规则配置**
- **安全组策略**

## 10. 测试策略

### 10.1 单元测试
- 服务层逻辑测试
- 工具类测试
- 异常处理测试
- **存储适配器单元测试**
- **文件夹操作测试**

### 10.2 集成测试
- API接口测试
- Redis集成测试
- 文件操作测试
- **多存储后端集成测试**
- **Docker容器集成测试**

### 10.3 性能测试
- 并发上传测试
- 大文件上传测试
- 系统负载测试
- **多存储后端性能对比测试**
- **文件夹上传下载性能测试**

### 10.4 安全测试
- 取件码安全测试
- 文件路径安全测试
- 异常处理测试
- **容器安全测试**
- **存储后端安全测试**

### 10.5 兼容性测试
- 不同存储后端兼容性测试
- 不同Docker环境测试
- 前端组件兼容性测试

## 11. 扩展规划

### 11.1 二期实现
- ✅ **文件夹上传功能**
- ✅ **多种文件存储系统支持**
- ✅ **Docker和Docker Compose部署**
- **存储策略管理和监控**

### 11.2 短期扩展（v2.1）
- 存储后端性能优化
- 容器化部署增强
- 监控告警系统完善
- 前端界面改进

### 11.3 中期扩展（v3.0）
- 分布式部署支持
- CDN集成
- 用户认证和权限管理
- 多租户支持

### 11.4 长期扩展（v4.0）
- 微服务架构改造
- 智能文件处理
- AI驱动的存储优化
- 边缘计算支持

## 12. 附录

### 12.1 术语表
- **取件码**：6位随机码，用于文件访问
- **分片**：大文件分割后的小文件块
- **上传会话**：一次文件上传的上下文信息
- **过期时间**：文件或会话的有效期限
- **存储后端**：文件存储系统的抽象（新增）
- **存储策略**：多存储后端的管理策略（新增）

### 12.2 相关文档
- [API接口文档](README.md#api文档)
- [部署指南](deploy/README.md)
- [开发规范](CONTRIBUTING.md)
- [Docker部署指南](docker/README.md)（新增）
- [存储配置指南](config/storage/README.md)（新增）

### 12.3 版本历史
| 版本 | 日期 | 描述 |
|------|------|------|
| v1.0 | 2024-12 | 初始版本 |
| v1.1 | 2024-12 | 增加分片上传功能 |
| v2.0 | 2024-12 | 二期功能：文件夹上传、多存储系统、Docker部署 |

---

**文档维护**：项目开发团队  
**最后更新**：2024年12月  
**文档状态**：v2.0 正式版